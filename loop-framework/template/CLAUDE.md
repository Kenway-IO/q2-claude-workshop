# CLAUDE.md

<!-- Day 1: repo memory. Fill in every section. Delete what doesn't apply. -->

## Stack

<!-- Languages, frameworks, versions. e.g. "Node 22, Next.js 15, Postgres 16" -->

## Test commands

<!-- Exact commands, copy-pasteable. -->
- Run all tests: `<command>`
- Run one test file: `<command> <path>`
- Lint: `<command>`
- Type check: `<command>`

## Code style

<!-- Only rules a linter can't catch. e.g. "prefer early returns", "no default exports" -->

## Release rules

<!-- What Claude may and may not do. e.g.: -->
- Never push to `main` directly — always a PR.
- Never deploy. Deploys are human-triggered.
- Never edit migration files that are already merged.

## Gotchas

<!-- Things that bite newcomers. e.g.: -->
<!-- - "Tests need the dev DB running: `docker compose up -d db`" -->
<!-- - "The /api/legacy routes are load-bearing, don't refactor them" -->
